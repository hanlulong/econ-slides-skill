% Interface test deck: exercises every element of the econ-slides semantic
% interface. Must compile cleanly under all bundled themes:
%   xelatex "\def\ecoslidestheme{econ-slides-house}\input{interface-test.tex}"
% Defaults to the house theme when compiled directly.
\ifdefined\ecoslidestheme\else\def\ecoslidestheme{econ-slides-house}\fi
\documentclass[11pt, aspectratio=169]{beamer}
% stock-theme mode: \def\ecoslidesstocktheme{Madrid} + econ-slides-compat
\ifdefined\ecoslidesstocktheme\usetheme{\ecoslidesstocktheme}\fi
\usepackage{\ecoslidestheme}

\title[Broadband and productivity]{Broadband Expansion and Small-Firm Productivity}
\subtitle{Evidence from a Staggered Municipal Rollout}
\author[One \& Two]{Author One \and Author Two}
\date[July 2026]{July 2026}

\begin{document}

% ---- Title (plain, de-counted) ----
\begin{frame}[plain]
  \titlepage
  \vfill\centering\scriptsize\textit{Disclaimer: synthetic test content only.}
\end{frame}
\addtocounter{framenumber}{-1}

% ---- Assertion title + subtitle + bullets + KeyIdea + graycite ----
\begin{frame}{Broadband rollout was staggered across 290 municipalities}
  \framesubtitle{Rollout reached 80 percent of municipalities between 2011 and 2017}
  \begin{itemize}
    \item Staggered adoption creates treated and not-yet-treated comparisons
      \begin{itemize}
        \item 284 municipalities connected between 2011 and 2018
        \item firms cannot choose their municipality's slot
      \end{itemize}
    \item \KeyIdea{Timing is plausibly exogenous} given region trends \graycite{(Autor '13; Hjort '19)}
      \begin{itemize}
        \item engineering constraints, not local demand, set the order
      \end{itemize}
    \item \RunIn{Design:} event study with a cohort-robust estimator
      \begin{itemize}
        \item placebo and clustering variants live in the appendix
      \end{itemize}
  \end{itemize}
\end{frame}

% ---- Result box + RunIn + math with \Et ----
\begin{frame}{Two headline results survive every specification}
  \begin{ResultBox}[Main result]
    A one-year exposure to broadband raises small-firm labor productivity by
    $3.4\%$ (s.e.\ $0.6$); effects concentrate in communication-intensive sectors.
  \end{ResultBox}
  \medskip
  \RunIn{Mechanism:} consistent with reduced coordination costs, since
  $\Et\,[y_{it+1}] $ rises only where task outsourcing is feasible.
  \begin{itemize}
    \item communication-intensive firms respond first and most
    \item \RunIn{Prediction:} the interaction term should be positive
  \end{itemize}
\end{frame}

% ---- Regression table: booktabs + threeparttable + highlighted column ----
\begin{frame}{The estimate is stable across specifications}
  \framesubtitle{Log labor productivity, firm and year fixed effects}
  \centering
  \resizebox{0.72\textwidth}{!}{%
  \begin{threeparttable}
    \begin{tabular}{lccc}
      \toprule
      & (1) OLS & (2) +Controls & \textcolor{cAccentA}{(3) +Region trends} \\
      \midrule
      Broadband $\times$ Post & 0.041*** & 0.036*** & \textcolor{cAccentA}{\textbf{0.034***}} \\
       & (0.009) & (0.008) & \textcolor{cAccentA}{(0.008)} \\
      \midrule
      Controls & No & Yes & Yes \\
      Region trends & No & No & Yes \\
      Observations & 148,220 & 148,220 & 148,220 \\
      \bottomrule
    \end{tabular}
    \begin{tablenotes}\footnotesize
      \item Standard errors clustered by municipality. *** $p<0.01$.
    \end{tablenotes}
  \end{threeparttable}}
  \TakeawayWithNav[\ExhibitReadingGapRoomy]{One standard deviation of exposure moves productivity by 0.7 percent.}
  \hypertarget{main:robust}{}%
  \PlaceNav{\hyperlink{app:robust}{\beamergotobutton{Robustness}}}
\end{frame}

% ---- Overlay reasoning test (handout-safe: exactly one variant survives) ----
\begin{frame}{The effect builds over three years, then stabilizes}
  \begin{overlayarea}{\textwidth}{0.55\textheight}
    \only<1|handout:0>{\centering\itshape (Figure placeholder: pre-trend coefficients near zero)}
    \only<2|handout:1>{\centering\itshape (Figure placeholder: post coefficients rising to 0.034)}
  \end{overlayarea}
  \begin{itemize}
    \item<1-> Pre-treatment coefficients are small and not significant
    \item<2-> \KeyIdea{Effects emerge with a two-year lag} — consistent with adoption delays
  \end{itemize}
\end{frame}

% ---- Appendix: divider, A-numbering, Back button ----
\AppendixStart

\begin{frame}{Robustness: alternative clustering and estimators}
  \hypertarget{app:robust}{}%
  \begin{itemize}
    \item Municipality clustering roughly doubles standard errors
      \begin{itemize}\item the displayed conclusion remains unchanged\end{itemize}
    \item Cohort-robust estimators give $0.031$--$0.036$
      \begin{itemize}\item estimates remain close to the preferred column\end{itemize}
    \item Assignment-level inference is the conservative default
      \begin{itemize}\item dependence follows the municipality rollout\end{itemize}
  \end{itemize}
  \begin{ResultBox}[What this backup establishes]
    The synthetic estimate is numerically stable across the listed alternatives.
  \end{ResultBox}
  \BackButton{main:robust}
\end{frame}

\end{document}
